{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window only; filesystem and network operations are validated in Rust.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
